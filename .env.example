# Neon pooled PostgreSQL connection string. Never prefix this key with VITE_.
DATABASE_URL=

# Used to sign secure, HTTP-only browser sessions. Generate a random value of 32+ bytes.
SESSION_SECRET=

# The sole initial administrator. Password must be a bcrypt hash, never plain text.
ADMIN_EMAIL=
ADMIN_PASSWORD_HASH=
